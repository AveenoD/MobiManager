/**
 * @deprecated Prefer `@/lib/otel-trace` for new code — this file re-exports trace helpers only.
 * Node SDK lives in `otel-init.ts` (loaded from instrumentation.ts only).
 */
export * from './otel-trace';
